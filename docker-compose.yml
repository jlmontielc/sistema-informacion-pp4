services:
  backend-flask:
    build: ./backend-flask
    expose:
      - "5000"
    env_file: ./backend-flask/.env
    restart: unless-stopped

  backend-node:
    build: ./backend-node
    ports:
      - "3000:3000"
    env_file: ./backend-node/.env
    environment:
      - FLASK_IA_URL=http://backend-flask:5000
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        - REACT_APP_API_URL=/api
    ports:
      - "80:80"
    depends_on:
      - backend-node
      - backend-flask
    restart: unless-stopped
